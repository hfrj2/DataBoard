<UserControl x:Class="DataBoard.Views.LineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataBoard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
              DataContext="{Binding Source={StaticResource Locator}, Path=Line}"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding Lines}" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="编号" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="生产线" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="添加者" Binding="{Binding UserInfo.Name}"/>
                <DataGridTextColumn Header="添加日期" Binding="{Binding InsertDate}"/>
                <DataGridTemplateColumn Header="操作">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="修改" Margin="5" Style="{StaticResource DataGrid_TextBlock}"/>
                                <TextBlock Grid.Column="1" Text="删除" Margin="5" Style="{StaticResource DataGrid_TextBlock}"/>
                            </Grid>
                            
                        </DataTemplate>
                        
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="添加" Tag="&#xf055;"  
                    Margin="15" Style="{StaticResource LineView_ButtonStyle}" 
                    FontSize="16"
                    Command="{Binding OpenAddLineWindowCommand}"
                    />
        </StackPanel>
    </Grid>
</UserControl>
